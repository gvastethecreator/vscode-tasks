# Status Bar Tasks fixture

Open this folder with **Run Extension**.

- `echo` is pinned on the status bar.
- `hidden` stays in the task menu.
- `readme` is pinned only when this README is active.
- `npm hello` and `npm nested` share a script name and stay distinct in the menu.
- `server` is a background task.
