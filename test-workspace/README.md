# Status Bar Tasks fixture

Open this folder with **Run Extension**.

- `echo` stays on the status bar.
- `hidden` does not.
- `readme` appears when this README is the active editor.
- `npm hello` and `npm nested` share a script name and must both show.
- `server` is a background task.
